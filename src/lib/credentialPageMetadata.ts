import type { Metadata } from "next";

/** Shared robots metadata for login and credential-entry surfaces. */
export const credentialPageMetadata: Metadata = {
  robots: { index: false, follow: false },
};
